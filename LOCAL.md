# 新版周看板：本地运行

这份部署来自 `weeklyboard-remake.zip`，与旧版、iPhone 工程分别存放，没有合并旧版排版或迁移旧数据。随包 README 混有旧版说明；本地启动请以本文为准。

## 现在怎么用

打开 <http://127.0.0.1:4173/>。

它只在这台 Mac 上运行，不是公网网址，也不向局域网开放。当前是本机体验模式，会自动进入演示工作区，不需要注册。普通看板和本地排程无需 API Key；没有配置或启用 DeepSeek。

## 下次怎么启动

1. 在 Finder 打开本文件所在的 `weeklyboard-remake` 文件夹。
2. 双击 `start.command`，它会启动网站和数据库服务，并打开浏览器。
3. 看到 `Weeklyboard is ready` 后可以关闭这个终端窗口，服务继续在后台运行。
4. 用完后双击 `stop.command` 停止服务。关闭网页本身不会停止服务。
5. 电脑重启后需要重新双击 `start.command`，没有设置开机自动运行。

如果 Finder 没有执行脚本，可以在终端运行：

```sh
cd "/Users/ericd/Documents/时间管理工具/weeklyboard-local/weeklyboard-remake"
/bin/zsh start.command
```

停止与检查状态：

```sh
./.local-tools/bin/node scripts/local.mjs stop
./.local-tools/bin/node scripts/local.mjs status
```

## 为什么有两个服务

- `127.0.0.1:4173`：网页界面，提供构建好的 React 页面。
- `127.0.0.1:8787`：本地 API，读写 SQLite 数据库。
- 两个服务都需要运行。直接双击 `index.html` 不适用于这个版本。
- 为避免每次启动换网址，端口保持固定。脚本重复运行不会重复启动；如果端口被其他软件占用，会报错而不会关闭其他程序。

## 数据放在哪里

- `server/weeklyboard.db`：账号、任务、日程、草稿等数据库。
- `server/.env`：本地配置和随机生成的令牌签名密钥，不要公开上传。
- `.local-run/`：运行日志与进程信息。
- `.local-tools/`：为这台 Apple Silicon Mac 准备的 Node.js/npm，无需修改系统安装。

关闭页面、停止服务不会主动删除数据库。重要内容仍建议使用页面里的备份导出。若手工复制数据库，先停止服务；保留整个 `server` 数据目录及可能出现的数据库附属文件。旧版浏览器里的数据不会自动出现在新版，这次没有做旧数据格式迁移。

当前 `DEBUG_MODE=true` 是本机体验模式，不代表面向其他人的安全登录部署。请不要将端口公开到互联网，也不要公开上传 `.env` 或数据库。

## 改源代码后更新页面

平时只需启动脚本，不用重装依赖或重新构建。修改 `src` 后，在项目目录运行：

```sh
export PATH="$PWD/.local-tools/bin:$PATH"
node .local-tools/npm/bin/npm-cli.js run build
```

刷新浏览器即可加载新的页面。修改后端代码后，用停止、启动脚本重启。

检查随包后端测试：

```sh
./.local-tools/bin/node --test server/test/*.test.js
```

如果需要重新安装依赖，先停止服务，再运行：

```sh
export PATH="$PWD/.local-tools/bin:$PATH"
node .local-tools/npm/bin/npm-cli.js ci --no-audit --no-fund
node .local-tools/npm/bin/npm-cli.js ci --prefix server --no-audit --no-fund
node .local-tools/npm/bin/npm-cli.js run build
```

## 排查启动失败

先查看 `.local-run/api.log` 和 `.local-run/website.log`。如果页面能打开但内容加载失败，检查两个服务的状态。移动项目目录前先停止服务，保留数据库，再从新位置启动。不要通过删除数据库解决普通启动错误。

## 番茄钟统计

- 顶部“番茄钟”开关控制统计区、任务池番茄钟数字及每日/分类进度。开关按账号保存在数据库中；关闭仅隐藏，不会删除计划或实际记录，也不会阻止日程继续保存。
- 统计区按当前周展示全部项目中的任务，以及非固定图层的独立日程；不会只统计正在看的图层。时间轴涂色仍由原有的分类小时统计展示，不作为命名任务的完成记录。
- 每个番茄钟为 30 分钟。计划容量不足 30 分钟的部分向上取整；实际完成数量只计完整的 30 分钟，不足部分在最后一格里浅色显示。实际分钟仍完整保留，例如 45 分钟显示 1 个完整番茄钟及半格。
- 每天恰好展示计划容量或已记录实际容量所需的方块，不在末尾额外加一个空格；无安排的日期不展示“0 个番茄钟”。
- 勾选会记录到选中的第几个番茄钟，优先使用该任务当天的计划时间，再寻找当天实际日程中的空位。再次点击已填充的格子，会取消从该格开始的当天记录，保留预计日程。取消是时间记录操作，不只是改变图标。
- 实际图层里修改时长、移动、删除日程后会重新计算番茄钟；实际完成可以超过计划，但不会把计划数字随之增加。超过 100% 后进度条进入下一种颜色。
- 计划栏仅接受整数，不能低于预计/一次性图层已安排时间需要的容量。提高计划会增加任务池的“还需安排”数量，不会自动挪动日程。
- 从任务池拖出的日程保存明确的任务关联；日程编辑面板可以调整“对应任务”。旧日程只在同账号存在唯一同名任务时归入该任务，避免不同项目的同名任务混在一起。
- 没有指定周或设置为重复的历史实际记录，可以展示统计，但需要先在日程里明确设为仅本周后才能用格子修改，避免一次取消影响其他周。
- 新的计划、开关设置、日程关联都包含在页面的 SQL JSON 备份中。番茄钟是统计功能，不是后台倒计时或通知功能。

## 日历订阅

1. 顶部点“订阅日历”，填写学校提供的 HTTPS 或 webcal 链接、名称和显示时区。NMH 课表选择 `America/New_York`。
2. 添加时可以选择图层，默认“一次性”。每份订阅的图层可在管理面板随时更改，已有日程一并移动，后续同步保持该选择。用上一周、下一周浏览对应日期。选择“实际”会按实际日程处理；普通课程建议用“一次性”或“每周固定”。全天事项显示在日期下方，不占用时间轴或计入番茄钟。
3. 后端运行期间每 30 分钟同步一次；重启时补同步到期的订阅。页面每分钟检查同步状态，也可点订阅旁的刷新图标立即同步。电脑休眠、断网或后端关闭时无法拉取新的课表。
4. 更新按日历 UID 和重复实例匹配，不会因反复刷新生成重复课程。源日历修改会更新课程；未来取消的日程会移除。滚动订阅不再提供的过去日程仍保留。失败时保留已有数据。
5. 订阅日程由来源日历维护，点击可查看详情。取消订阅默认保留日程，保留后可在本地编辑；勾选“同时删除”则只删除这份订阅的日程，不影响其他手动安排。

ICS 文件导入也已改为标准解析，支持时区、跨天、重复规则、排除日期和例外修改，重复导入同一个 UID 会更新。重复规则展开为最近 4 周至未来 183 天内的实例；普通有日期的日程保留文件提供的范围。每份文件上限 2 MB / 5000 个日程时间段。

订阅是单向读取，不会修改学校日历。请把带访问凭证的链接当作密码保管：链接只保存在本机数据库，不放进源码、日志或页面 JSON 备份。备份包含已经同步的日程；恢复备份会将它们保留为独立日程并取消原订阅，继续同步需重新添加链接。不要公开分享数据库文件。当前体验版仍只监听本机地址，不可直接用于公开服务。

解析使用 [node-ical](https://github.com/jens-maus/node-ical)，后台抓取直接连接订阅网站，不经过公共 CORS 代理。仅允许公共 HTTPS 地址，拒绝内网地址及跨站重定向，设有超时、大小及解析资源限制。

## Canvas 作业与考试提醒

- 粘贴 Canvas / Instructure 订阅链接时，用途会自动选为“作业 / 考试提醒”；其他日历也可手动选择这个用途。原学校课表继续作为日程，不会变成作业提醒。
- 来源提供的每个截止事项生成一条提醒，保留标题、课程、说明、原始内容链接和日期。带具体时刻的事项按所选时区显示；只有日期的事项显示“当日截止”，不会擅自补成 23:59。
- 同一份提醒同时出现在左侧“截止警示”、周看板日期下方的“截止”栏，以及月历“截止雷达”。月历读取该月全部提醒，不限于当前选中的周。周看板中的订阅提醒跟随所属图层；左侧列表保留本周所有图层的提醒。
- 点提醒可查看完整说明和课程链接。标题含 exam、quiz、test、midterm 或相应中文关键词时初始归为考试，其余归为作业；详情中的“提醒类型”可手动更正，更正后不会被自动同步覆盖。
- 完成/取消完成在各处即时同步，并保存在数据库和 JSON 备份中。该标记只用于本工具，不代表已经在 Canvas 提交作业，也不会回写 Canvas。
- 截止时刻不是学习时长，提醒不会占据 24 小时时间块，也不计入番茄钟。来源更新截止日期时会移动原提醒，不会复制一份；完成状态仍保留。取消订阅默认保留提醒，勾选同时删除时只移除该订阅关联内容。
- 这是周看板内的可视提醒，不是手机或电脑系统推送。
