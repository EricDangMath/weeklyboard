{
  "name": "weeklyboard",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first weekly planner with planned and actual time tracking.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check app.js",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  }
}
